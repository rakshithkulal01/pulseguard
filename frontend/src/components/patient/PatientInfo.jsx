import { formatBloodGroup, formatGender, getInitials } from "../../utils/formatters";
export default function PatientInfo({ patient }) { return <section className="patient-info-section"><div className="patient-title-row"><div className="patient-avatar">{getInitials(patient.fullName || patient.name)}</div><div><h1>{patient.fullName || patient.name}</h1><p>Patient Profile</p></div></div><div className="patient-details"><Info label="Age" value={`${patient.age ?? "N/A"} years`} /><Info label="Gender" value={formatGender(patient.gender)} /><Info label="Blood Group" value={formatBloodGroup(patient.bloodGroup || patient.blood)} /></div></section>; }
function Info({ label, value }) { return <div className="patient-detail"><div className="detail-icon">•</div><div><span>{label}</span><strong>{value}</strong></div></div>; }
